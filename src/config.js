const config = {}

// Propeds private
let name = 'thiagobritto'
let title = 'Thiago Britto'
let version = '0.1.0'

// Methods public
config.getName = () => name
config.getTitle = () => title
config.getVersion = () => version

export default config